import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  spacing: 4,
  palette: {
    primary: {
      main: '#CD4436',
    },
    secondary: {
      main: '#dcddd8',
    },
    action: {
      focus: '#CD4436',
    },
  },
})

export default theme
